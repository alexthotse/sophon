- `sophon invite` command to invite users to an org
- `sophon users` command to list users and pending invites for an org
- `sophon revoke` command to revoke an invite or remove a user from an org
- `sophon sign-in` fixes
- Fix for context update of directory tree when some paths are ignored
- Fix for `sophon branches` command showing no branches immediately after plan creation rather than showing the default 'main' branch